# Prixelart WebApp Frontend

To start, just clone both [frontend](https://github.com/CapuzR/prixelart-frontend) and [backend](https://github.com/CapuzR/prixelart-backend) repo, configure your env variables and run:

```bash
npm start
```
